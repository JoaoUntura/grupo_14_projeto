<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Recuperar Senha</title>
    <link rel="stylesheet" href="../css/bootstrap.min.css">
    <link rel="stylesheet" href="../css/personalizado.css">
</head>

<body class="bg-light">

<div class="container d-flex justify-content-center align-items-center vh-100">
    <div class="card p-4 shadow" style="max-width: 420px; width: 100%;">
        
        <h3 class="text-center mb-4 page-title">Recuperação de Senha</h3>

        <% if (erro) { %>
            <div class="alert alert-danger"><%= erro %></div>
        <% } %>

        <% if (msg) { %>
            <div class="alert alert-success"><%= msg %></div>
        <% } %>


        <% if (!etapa || etapa === 1) { %>

            <form action="/recuperarSenha" method="POST">

                <div class="mb-3">
                    <label class="form-label custom-label">Digite seu e-mail</label>
                    <input type="email" class="form-control custom-input" name="email"
                           placeholder="seuemail@gmail.com" required>
                </div>

                <button class="custom-button w-100" type="submit">
                    Enviar código
                </button>
            </form>

        <% } %>


        <% if (etapa === 2) { %>

            <form action="/novaSenha" method="POST">

                <input type="hidden" name="email" value="<%= email %>">

                <div class="mb-3">
                    <label class="form-label custom-label">Código recebido por e-mail</label>
                    <input type="text" class="form-control custom-input" name="codigo"
                           placeholder="6 dígitos" required>
                </div>

                <div class="mb-3">
                    <label class="form-label custom-label">Nova senha</label>
                    <input type="password" class="form-control custom-input" name="novaSenha"
                           placeholder="Digite a nova senha" required>
                </div>

                <button class="custom-button w-100" type="submit">
                    Alterar senha
                </button>

            </form>

        <% } %>

    </div>
</div>

</body>
</html>